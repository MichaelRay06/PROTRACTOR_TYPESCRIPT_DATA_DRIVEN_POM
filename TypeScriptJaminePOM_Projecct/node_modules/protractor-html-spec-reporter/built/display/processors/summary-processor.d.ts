import { CustomReporterResult } from "../../custom-reporter-result";
import { ExecutionMetrics } from "../../execution-metrics";
import { DisplayProcessor } from "../display-processor";
export declare class SummaryProcessor extends DisplayProcessor {
    displaySummary(spec: CustomReporterResult, log: string, metrics: ExecutionMetrics): string;
}
