import { CustomReporterResult } from "../../custom-reporter-result";
import { DisplayProcessor } from "../display-processor";
export declare class HtmlProcessor extends DisplayProcessor {
    displayJasmineStarted(): string;
    displaySuite(suite: CustomReporterResult, log: string): string;
    displaySuiteDone(suite: CustomReporterResult, log: string): string;
    displaySpecStarted(suite: CustomReporterResult, log: string): string;
    displaySpecDone(spec: CustomReporterResult, log: string): string;
    displaySuccessfulSpec(spec: CustomReporterResult, log: string): string;
    displayFailedSpec(spec: CustomReporterResult, log: string): string;
    displayPendingSpec(spec: CustomReporterResult, log: string): string;
    displaySummary(spec: CustomReporterResult, log: string): string;
    displayJasmineDone(spec: CustomReporterResult, log: string): string;
    /**
     * Get a browser screenshot from protractor browser and save on filesystem.
     * Return the filename of the saved screenshot
     */
    private getScreenshot(spec);
    /**
     * Create stylesheets links for imported and linked stylesheets from configuration
     */
    private generateStylesheetLinks();
    /**
     * Generate the Spec table row content
     */
    private decorateSpec(spec, log, link, status, withErrorMessages?);
    private generateDurationTD(spec);
    private generateStatusTD(spec, status, withErrorMessages);
    private getErrorMessages(spec);
}
